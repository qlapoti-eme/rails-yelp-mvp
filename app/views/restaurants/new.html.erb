<div class="container">
  <%= simple_form_for(@restaurant) do |r| %>
    <%= r.input :name %>
    <%= r.input :address %>
    <%= r.input :category %>
    <%= r.submit "Create new restaurant" %>
  <% end %>
</div>
